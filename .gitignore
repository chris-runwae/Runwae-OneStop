node_modules
.turbo
.next
dist
out
*.tsbuildinfo
.env.local
.env*.local
.DS_Store

# Local worktrees
.worktrees/

# Claude Code local state (sessions, worktrees, plans)
.claude/

# Apple .p8 keys — keep these well away from git
*.p8
